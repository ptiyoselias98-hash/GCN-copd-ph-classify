# Local analysis-only environment (Windows Python 3.14, Round 3 snapshot)
# The local scripts never touch kimimaro or torch; they compute protocol
# labels, lung-phenotype v2 features, fold-level CIs, and clustering.
numpy>=2.0
pandas>=2.1
scipy>=1.12
scikit-learn>=1.5
nibabel>=5.4
matplotlib>=3.8
umap-learn>=0.5.7
